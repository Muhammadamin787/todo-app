// import * as Appwrite from 'node-appwrite';

// const client = new Appwrite.Client();
// client.setEndpoint(String(process.env.NEXT_PUBLIC_APPWRITE_ENDPOINT))
//     .setProject(String(process.env.NEXT_PUBLIC_PROJECT_ID))
//     .setKey(String(process.env.NEXT_PUBLIC_API_KEY))

// const account = new Appwrite.Account(client); // Instantiate the Account service

// export { client, account }; // Export the client and account for use in other files